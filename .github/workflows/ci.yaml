name: Print Message

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print a custom message
        run: echo "🎉 Hello from GitHub Actions! This is your CI workflow speaking."
